// const handleNext = (page, pageCount, setPage) => {
//     setPage((p) => (p === pageCount ? p : p + 1));
// };
  
//   const handlePrevious = (page, setPage) => {
//     setPage((p) => (p === 1 ? p : p - 1));
// };

// export { handleNext,handlePrevious }